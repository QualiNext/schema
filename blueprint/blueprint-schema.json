{
  "$id": "http://example.com/example.json",
  "type": "object",
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "spec_version": {
      "$id": "/properties/spec_version",
      "type": "integer",
      "title": "The Spec_version Schema",
      "default": 1,
      "examples": [1],
      "enum" : [1]
    },
    "kind": {
      "$id": "/properties/kind",
      "type": "string",
      "title": "The Kind Schema ",
      "default": "",
      "examples": [
        "blueprint"
      ],
      "enum": [ "blueprint" ]
    }
  },
  "required": ["spec_version", "kind"]
}
